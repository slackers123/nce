fn main() -> u8 {
    let a: u8 = 0;

    while a < 10 {
        a = a + 1;
    }

    return fib(a);
}

fn fib(n: u8) -> u8 {
    if n <= 1 {
        return n;
    }

    return fib(n-1) + fib(n-2);
}
