fn main() -> i64 {
    let a = 0;
    while a < 10 {
        a = a + 1;
    }

    return fib(a);
}

fn fib(n: u64) -> u64 {
    if n <= 1 {
        return n;
    }

    return fib(n-1) + fib(n-2);
}
